{
  "name": "rap_fe",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && npm run start",
    "start": "next start -p $PORT",
    "build:prod": "next build && PORT=3000 npm run start",
    "build:test": "next build && PORT=3001 npm run start",
    "start:prod": "pm2 start yarn --name rap-fe-prod -- build:pord",
    "start:test": "pm2 start yarn --name rap-fe-test -- build:test"
  },
  "dependencies": {
    "lib-flexible": "^0.3.2",
    "next": "10.0.3",
    "postcss-px2rem": "^0.3.0",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "sass": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^14.14.10",
    "@types/react": "^17.0.0",
    "typescript": "^4.1.2"
  }
}
